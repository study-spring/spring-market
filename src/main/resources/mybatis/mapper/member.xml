<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="project.springmarket.mybatis.MemberMapper">

    <resultMap id="memberResultMap" type="project.springmarket.model.member.Member">
        <result column="member_no" property="memberNo"/>
        <result column="id" property="memberID"/>
        <result column="pw" property="memberPW"/>
        <result column="address_no" property="addressNo"/>

    </resultMap>


    <select id="findById" parameterType="string" resultMap="memberResultMap">
        select * from member where id = #{id}
    </select>

    <insert id="createMember"  useGeneratedKeys="true" keyColumn="member_no"
            keyProperty="memberNo" parameterType="map">
        INSERT INTO member (id, pw, address_no) VALUES (#{id}, #{pw}, #{addressNo});
    </insert>

</mapper>